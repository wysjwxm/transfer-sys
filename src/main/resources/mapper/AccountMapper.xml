<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wysjwxm.infrastructure.persistence.mybatis.AccountMapper">

    <select id="findByIdForUpdate" resultType="com.wysjwxm.domain.account.Account">
        SELECT user_id, balance
        FROM account
        WHERE user_id = #{userId}
        FOR UPDATE
    </select>

    <select id="findById" resultType="com.wysjwxm.domain.account.Account">
        SELECT user_id, balance
        FROM account
        WHERE user_id = #{userId}
    </select>

    <update id="updateBalance">
        UPDATE account
        SET balance     = #{balance},
            update_time = CURRENT_TIMESTAMP
        WHERE user_id = #{userId}
    </update>

</mapper>
