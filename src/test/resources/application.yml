# 测试用内存库，避免共享 dev 的文件库数据；schema/data 由 spring.sql.init 重建
spring:
  datasource:
    url: jdbc:h2:mem:transfer_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  h2:
    console:
      enabled: false
